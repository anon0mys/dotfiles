Read the following files to build a mental model of this project before we begin work:

1. `package.json` or `pyproject.toml` (dependencies, scripts, project metadata)
2. `CLAUDE.md` in the current directory (project-specific instructions)
3. `README.md` if present (project overview)
4. The main entry point (`src/index.ts`, `src/main.ts`, `index.ts`, `main.py`, or similar)
5. Any top-level architecture docs in `docs/` if present

After reading, output:
- **Stack**: languages, frameworks, key dependencies
- **Architecture**: how the system is structured (2-3 sentences)
- **Entry points**: where execution starts, key modules
- **Test/build commands**: how to run tests and build
- **Gotchas**: anything non-obvious from CLAUDE.md or README

Keep the summary under 15 bullet points. Then confirm ready.
